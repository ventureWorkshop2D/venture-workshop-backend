spring:
    application:
        name: homeListener



    jackson:
        serialization:
            write-date-keys-as-timestamps: false

    profiles:
        include: credential



logging:
    level:
        com:
            project:
                homeListener: DEBUG